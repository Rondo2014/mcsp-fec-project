{
  "name": "mcsp-fec-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm:dev:*\"",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "host": "concurrently \"npm:host:*\"",
    "host:client": "npm run dev -- --host --workspace=client",
    "host:server": "npm run host --workspace=server",
    "postinstall": "cross-env OS=unix npm install --prefix client && cross-env OS=unix npm install --prefix server"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}
